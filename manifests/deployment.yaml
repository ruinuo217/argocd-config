apiVersion: apps/v1
kind: Deployment
metadata:
  name: fastapi-deploy
spec:
  replicas: 3
  selector:
    matchLabels:
      app: fastapi
  template:
    metadata:
      labels:
        app: fastapi
    spec:
      containers:
        - name: fastapi
          image: ruinuo/docker-fastapi:8466bfeb9dee0c0600475b318f1c76b922adcdf3
          ports:
            - containerPort: 8000
          resources:
            requests:
              cpu: "100m"       # 最低需求 0.1 核
            limits:
              cpu: "500m"       # 上限 0.5 核
